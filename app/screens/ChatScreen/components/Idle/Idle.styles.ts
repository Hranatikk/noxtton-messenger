import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  input: {
    borderColor: "#ddd",
    borderRadius: 8,
    borderWidth: 1,
    flex: 1,
    height: 44,
    paddingHorizontal: 12,
  },
  row: {
    alignItems: "center",
    alignSelf: "stretch",
    flexDirection: "row",
    gap: 8,
    marginBottom: 12,
  },
})
